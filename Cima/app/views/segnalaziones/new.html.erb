<%= form_with(model: @segnalazione, url: segnalaziones_path, method: :post) do |form| %>
  <%= form.hidden_field :post_id %>
  <%= form.label :motivo %>
  <%= form.text_area :motivo %>
  <%= form.submit 'Invia Segnalazione' %>
<% end %>
